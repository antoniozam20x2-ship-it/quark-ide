{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install -g opencode-ai@1.18.20 && NODE_ENV=development npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start",
    "restartPolicyType": "ON_FAILURE"
  }
}
