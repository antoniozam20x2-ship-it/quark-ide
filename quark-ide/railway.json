{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd quark-ide && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd quark-ide && npm run start",
    "restartPolicyType": "ON_FAILURE"
  }
}
